import 'package:flutter/material.dart';

class TrackViewScreen extends StatelessWidget {
  const TrackViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xff9E2216),
      body: CustomScrollView(
        slivers: [],
      ),
    );
  }
}
